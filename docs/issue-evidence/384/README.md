# Issue 384 evidence

These crops isolate the exposed desktop margin beside Shufflepuck Cafe's stable cafe window.

- `systemless.png`: transition bytes remain visible as indexed-color slivers in the desktop area.
- `reference.png`: the same cafe scene has a clean desktop margin in the reference runtime.

Each crop retains the left edge of the correctly rendered game window for scene and coordinate context.
